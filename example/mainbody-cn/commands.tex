本模板定义了如下的额外的功能性命令\footnote{关于旧版本中的\texttt{\textbackslash shserifbold}的命令的这行内容被我注释掉了，因为我觉得这个命令的名字实在是太丑了，虽然它的功能很实用，但我觉得不够优雅。而且另一方面，这个命令也存在其他问题。比如用户必须事先在计算机上安装了思源宋体才能使用这个命令，否则就会报错。但是这又会引入新的依赖。尤其是在Windows系统上，用户可能需要手动安装——确切来说，是手动下载压缩包、解压、右键选中字体确定“为所有用户安装”——思源宋体才能使用这个命令，这就增加了用户的使用难度和不便。再加上同样是在Winodws上，\LaTeX{}查找字体目录的实现经常会出问题。这导致将字体和文档类一起发布的设计变得极为困难。更何况这种特性还必须要在不同平台上测试才能确保其稳健性。存在诸多潜在的问题。

如果你先前使用本项目构建的文档中使用了这个命令，我建议你尽快将其替换为普通的\texttt{\textbackslash textbf}命令。除此之外，考虑到思源系列字体的实用性 (展示生僻字、提供更好的中文排版效果、避免版权纠纷等)，我计划在未来的版本中重新引入这个功能，提供全文级别的字体支持。并且会为它设计一个更优雅的命令名称。敬请期待！
}：

\begin{enumerate}
    \item \texttt{\textbackslash fig\{<图题>\}\{<图片文件名>\}\{<label内容>\}}：用于快速方便地插入图片。
    \item \texttt{\textbackslash subtitle\{\}}：用于为文档添加一个副标题是。
    \item \texttt{\textbackslash maketitlewithonecolabstract\{<摘要内容>\}}：在使用双列模式的情况下，同时实现插入摘要文本和生成文档标题。
    % \item \texttt{\textbackslash shserifbold}：使用思源宋体粗体，通常用在文档的大标题上——如果你不喜欢黑体作为文档总标题的话。
\end{enumerate}

\begin{remark}
  使用\texttt{twocolumn} 参数的情况下，文档的大标题会被默认安置在文档的一侧。如果您希望大标题横跨两栏，就必须使用\texttt{\textbackslash twocolumn}命令的参数栏将\texttt{\textbackslash maketitle}命令和\texttt{onecolabstract}环境一起包裹起来：

% tex-fmt: off
\begin{minted}{tex}
\twocolumn[
  \maketitle
  \begin{onecolabstract}
    摘要内容……摘要内容……摘要内容……
    \end{onecolabstract}
]
\end{minted}
% tex-fmt: on

  这将会十分不方便。因此，为了解决这个问题，我们定义了专门的
% tex-fmt: off
\begin{minted}{tex}
\maketitleabstracttwocol\{<摘要内容>\}
\end{minted}
% tex-fmt: on
  命令，来实现two column abstract一步处理。
\end{remark}